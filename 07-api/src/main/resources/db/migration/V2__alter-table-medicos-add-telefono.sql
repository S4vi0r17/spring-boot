alter table medicos add telefono varchar(20) not null;

-- `delete from flyway_schema_history where success = 0;`
-- Se usa para eliminar de la tabla Flyway todas las migraciones cuya ejecución falló.
